# 2026-08-17 — Alberto. Cypress ships (with our crypto in it); the ladder re-anchored on the release; the #18 review answered

Follows [`2026-08-16-al.md`](./2026-08-16-al.md).

## 1. Cypress released — and vti-tsp-js 0.2.0 is our noble HPKE

Glenn's release report (OpenVTC Updates): 56 days, 1,126 merges over 15
repos, TSP added, "Trust Tasks used for everything" — with Brendan and
Alberto credited for DTG 1.0 draft 1 and the DTG/Trust-Tasks alignment.
PR #116 merged the morning of the cut (Glenn's line-by-line RFC 9180 audit:
"all correct"; his consolidation commit `b086285` extended it into pnm-core,
verified locally 51/51 + 215/215), released as **`@openvtc/vti-tsp-js`
0.2.0** — npm description now "No WebCrypto, no WASM: runs in browsers,
Node, and React Native" — and **`pnm-core` 0.4.0**. The Keyring-driven
crypto is the release's TSP wire layer.

## 2. The Cypress boundary session

All pins advanced to the release (tags verified == main heads; tf → the
0.9.0 lock). **Full ladder green — no new upstream defects.** Forced
changes: ref-03b/03c probes extract from upstream `origin/main` with the
merged `__unsafeFixedEphemeralSk` options API; ref-06w3 moved `taskContext`
to the credential top level (review nit). npm tripwire baselines reset at
the lock (0.2.0 / 0.4.0 / 0.9.0). Full log:
`docs/spikes/cypress-boundary-findings.md`.

## 3. Geoff's first-pass review of #18 — answered on the #5 stack

Thirteen inline comments, one blocking: the outcome-evidence pairing assumed
`threadId == id`, but §4.9 lets an initiator mint. Fixed his way (option b):
pairing runs through the initiating document the holder already ships —
`terminal.threadId == (initiating.threadId ?? initiating.id)`. Worth noting
upstream independently took option (a) for the witness family
(`witness/session` conformance requires `threadId` equal to `id`); the two
compose. Also addressed: both broken anchors, the §7.3 single-form
allowance, the `digestMultibase`/VC-DM claim scoped honestly, the
bbs-2023 proof-set decision (selective disclosure requires a bbs-2023 base
proof, so SD-capable issuers issue a proof set; RDF-canonicalization caveat
stated; the ZKP section's format-agnostic bullet aligned), the SD/edge-
binding trade-off sentence, a visibly-id example value with recomputed
digest, the 0.3-header/0.4-changelog version note, the intro's mechanism
wording, and the term definition's re-point phrasing. One commit on
`task-digest-binding` → Mickens #5; reply to Geoff drafts after Brendan's
pass.

## 4. The queue (2026-08-17)

1. **Brendan reviews Mickens #5** (now: binding + §4.9.3 alignment + review
   response) — Alberto pings; on merge, #18 updates and the Geoff reply posts.
2. **Case-2 comment on #173** — drafted, still valid at the Cypress pins.
3. **Reference-crate PR** — re-verified still needed at the 0.9 lock.
4. **Prague plumbing** — pnm-core 0.4.0 channel chain, QR/VTA onboarding,
   the OpenVTC TS library when Glenn cuts it. GDC is two weeks out.

## 5. Brendan's build-up/testing phasing (Google doc, ~08-10), mapped to the ladder

His four steps, audited against what exists today:

| Phasing step | Coverage | Where |
|---|---|---|
| 1. Alice+Bob, DIDComm 1 (Credo-ts) | **Done** | ref-06v1 (12 checks), plus v1c/v1d/v1e on top |
| 2. …with credo mediator | **Done** | ref-06v1b (7 checks, production Keyring mediator) |
| 3. …with Cypress VTA/VTI (affinidi mediator, webvh hosting) | **Half** — the components are proven separately (ref-04: affinidi mediator, TSP legs; ref-05: self-hosted VTA serving its own did:webvh), but no rung yet runs the *trust-task exchange* through the Cypress stack end-to-end | gap → planned rung below |
| 4. Fallback to pinned version if needed | **Standing infrastructure** | `scripts/openvtc/PINS.json` + `sync-external.mjs` — pins are the fallback, advances are logged decisions |

The step-3 gap gets a planned rung: **ref-06x-cypress-stack** — Alice and
Bob as Credo agents enrolled against a Cypress VTA (webvh DIDs, affinidi
mediator), running the witnessed exchange on binding 0.2 + trust-tasks
0.9.0. It is also the natural spine of the Prague demo plumbing, so it
double-counts toward queue item 4.

## 6. ref-06x built and green (same day, later) — phasing step 3 closed

The full-stack rung runs: 14 checks against the Cypress release binary.
Composition held everywhere it could; the two joints that resist are now
*measurements*: Credo 0.6.3 has no `did:webvh` resolver
(`unsupportedDidMethod`; the ~20-line fetch-and-parse workaround is proven in
the rung itself — Keyring needs the adapter, and a Credo contribution is a
candidate), and the Cypress mediator is v2-only (live 404 for a v1 envelope —
by design; the dual-stack transport strategy is now measured, not assumed).
Discoverability for review: `tsp-reference/README.md` now opens with the
phasing map — each of Brendan's four steps → its rung(s), one `node run.mjs`
away. Findings ledger: `docs/spikes/cypress-boundary-findings.md` §ref-06x.
