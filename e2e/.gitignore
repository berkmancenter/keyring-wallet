# root .gitignore ignores "lib" (build output); e2e/lib is source — re-include it
!lib/
!lib/**
node_modules/
artifacts/
# ad hoc diagnostic dumps from manual/agent verification runs (e.g. artifacts-run1,
# artifacts-run2-badenv) — same shape as artifacts/, just not that exact name
artifacts-*/
