{
  "name": "ref-06w2-compat",
  "private": true,
  "type": "module",
  "description": "TSP reference rung 06w2 — compatibility evidence: the legacy witness dance and the trust-task dance share one crypto core (proven with the real compiled witness-server functions), a lossless translator, and one mixed-dialect session",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node run.mjs",
    "check": "node run.mjs --quiet"
  },
  "overrides": {
    "@openwallet-foundation/askar-shared": "0.6.0"
  },
  "dependencies": {
    "@credo-ts/askar": "0.6.3",
    "@credo-ts/core": "0.6.3",
    "@credo-ts/didcomm": "0.6.3",
    "@credo-ts/node": "0.6.3",
    "@openwallet-foundation/askar-nodejs": "^0.6.0",
    "@openvtc/trust-tasks": "0.6.0"
  }
}
