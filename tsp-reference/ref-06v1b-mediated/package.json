{
  "name": "ref-06v1b-mediated",
  "private": true,
  "type": "module",
  "description": "TSP reference rung 06v1b — the same didcomm-v1 binding carriage, but through the real Keyring Credo mediator (network hop, forward-wrapping, pickup)",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node run.mjs",
    "check": "node run.mjs --quiet"
  },
  "overrides": {
    "@openwallet-foundation/askar-shared": "0.6.0"
  },
  "dependencies": {
    "@credo-ts/askar": "0.6.3",
    "@credo-ts/core": "0.6.3",
    "@credo-ts/didcomm": "0.6.3",
    "@credo-ts/node": "0.6.3",
    "@openwallet-foundation/askar-nodejs": "^0.6.0"
  }
}
